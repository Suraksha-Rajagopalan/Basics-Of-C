#include <stdio.h>
// Here until the value of loop doesn't become zero the while loop's condition is true and hence gets executed.
int main(){
    int loop=10;
    while(printf("Hello ") && loop--); 
    return 0; 
    
}
